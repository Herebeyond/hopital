{% if value is not empty %}
    <div class="badge-list">
        {% for patient in value %}
            <a href="{{ ea_url().setController('App\\Controller\\Admin\\PatientCrudController').setAction('detail').setEntityId(patient.id) }}" 
               class="badge badge-primary mb-1" 
               title="Voir détails">
                {{ patient.nom }} {{ patient.prenom }} ({{ patient.ndossier }})
            </a>
        {% endfor %}
    </div>
    <small class="text-muted">{{ value|length }} patient(s)</small>
{% else %}
    <span class="text-muted">Aucun patient</span>
{% endif %}
